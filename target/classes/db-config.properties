# =============================================
# 数据库配置文件
# 请根据实际情况修改以下配置
# =============================================

# 数据库连接URL
# MySQL 示例: jdbc:mysql://host:port/database?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.url=jdbc:mysql://10.9.56.131:6000/ggetl?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true

# 数据库用户名
db.username=root

# 数据库密码
db.password=7aAHkV21

# JDBC驱动类名
db.driver-class-name=com.mysql.cj.jdbc.Driver

# =============================================
# 其他可选配置
# =============================================

# 连接池配置（可选，覆盖application.yml中的默认值）
db.pool.max-size=20
db.pool.min-idle=5
db.pool.connection-timeout=30000
